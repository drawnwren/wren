# wren-config

Typed declarative TOML configuration, command-schema validation, content-addressed
config bundles, sanitized session environments, and hash-fenced workspace trust.
The only conditional evaluator is `wren-grammar`'s closed expression language.
