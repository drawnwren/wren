# wren-types

**Layer:** foundational core types. **Phase:** 0.

Defines IDs, revisions, byte-offset transactions, anchors, selections, and the
unstable mutation envelope. This crate must never depend on I/O frameworks,
Tokio, text backends, editor state machines, or presentation code.

