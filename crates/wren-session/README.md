# wren-session

Workspace authority and durable state: checksummed journal/recovery, persisted
mutation-id dedup, atomic multi-document mutations and state, lease fencing,
event retention/resume, client outbox, local recovery WAL, complete file
semantics, and transactional workspace edits/resource operations with
best-effort persist batches.
