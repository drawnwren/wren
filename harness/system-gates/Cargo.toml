[package]
name = "wren-system-gates"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
blake3.workspace = true
hdrhistogram.workspace = true
nix.workspace = true
serde_json.workspace = true
tempfile.workspace = true
wren-benchmark-support = { version = "0.1.0", path = "../benchmark-support" }
wren-provider = { version = "0.1.0", path = "../../crates/wren-provider", features = ["benchmarking"] }
wren-remote = { version = "0.1.0", path = "../../crates/wren-remote", features = ["benchmarking"] }
wren-text = { version = "0.1.0", path = "../../crates/wren-text", features = ["benchmarking"] }
wren-types = { version = "0.1.0", path = "../../crates/wren-types" }

[lints]
workspace = true
