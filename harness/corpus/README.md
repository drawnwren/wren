# wren-corpus

**Layer:** generated benchmark data and bake-off measurement utility. **Phase:**
0.

Generates reproducible normal, large, pathological, and Unicode-heavy inputs.
The 100 MB JavaScript and 8 MB one-line JSON files are build artifacts and are
not committed. Production crates must never depend on this harness.

